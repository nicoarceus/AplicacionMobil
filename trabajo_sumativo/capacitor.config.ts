import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'trabajo_sumativo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
